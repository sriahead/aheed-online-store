# Catalogue debt bucket: broken homepage rows, real featured flag, multi-image admin management (validation)

| Req | How to verify |
|-----|---------------|
| R1  | Read `lib/repositories/products.ts`: the `ProductRepository` interface has a new method (e.g. `list`) alongside `search`/`listByCategory`, and its implementation does not early-return on an empty string. Read `app/(storefront)/page.tsx`: neither of the two `Promise.all` calls invokes `.search(`. |
| R2  | `npm run preview`. `curl -s http://localhost:8787/ | grep -c "New Arrivals"` returns ≥1 (proves that row renders regardless of any `isFeatured` state). Pick one product not currently flagged `isFeatured`, note its name; confirm that name doesn't appear inside the "Featured Products" section of `curl -s http://localhost:8787/`. Sign in as `demo-store-admin`, edit that product, check the `isFeatured` checkbox, save. Re-fetch `/`: the product's name now appears inside the "Featured Products" section. |
| R3  | `grep -n "isFeatured" prisma/schema.prisma` shows a `Boolean @default(false)` field on `Product`. `ls prisma/migrations/ | grep -i featured` (or equivalent) shows a new migration folder. `npx prisma migrate status` reports no pending migrations — `npm run preview` does NOT apply migrations itself (`opennextjs-cloudflare build && ... preview` only); the migration must have been applied separately via `npm run db:migrate:dev` (or `db:migrate`) against `DIRECT_URL` before this check, per `CLAUDE.md`'s migration rules. |
| R4  | `grep -n "isFeatured" lib/catalogue-form.ts` shows it in the `ProductFormValues` interface, in a `checkbox(raw, "isFeatured")` call, and in the `PRODUCT_FIELDS` array. |
| R5  | `grep -n "isFeatured" components/staff/ProductForm.tsx` shows a `<Checkbox name="isFeatured" .../>` using the same component as the Halal/Fresh/Organic checkboxes. `grep -n "isFeatured" lib/repositories/products.ts` shows it written in both `createProductForVendor` and `updateProductForVendor`'s Prisma `data` objects. |
| R6  | Read `app/(storefront)/page.tsx`: the `ProductRow` with `title="Featured Products"` (not "Featured Halal Deals") is fed by R1's method called with an `isFeatured` filter; `grep -n "isHalal" app/(storefront)/page.tsx` shows no match on that line. |
| R7  | `grep -n "deleteObject" lib/storage.ts` shows it in the `StorageService` interface and in the implementation, calling the same `AwsClient` instance `putObject`/`headObject` use — no new import beyond `aws4fetch`. |
| R8  | Read `lib/repositories/products.ts`'s `addProductImage`: confirm the `isPrimary` ternary (true only when the product has zero existing images) and the `sortOrder` computation (max + 1, or 0). |
| R9  | Read `promoteProductImage`: confirm it runs inside a `$transaction` (via `getPrismaWs()`, matching `setPrimaryProductImage`'s pattern) and that it does not write `storageKey` on either row. |
| R10 | Read `removeProductImage`: confirm it calls `getStorage().deleteObject(...)` with the removed row's `storageKey`, and confirm the primary-reassignment branch (only when the removed row was primary and other rows remain, picks lowest remaining `sortOrder`). |
| R11 | Read `reorderProductImages`: confirm it compares the incoming id set against the product's current image ids before writing anything, and that `sortOrder` is written as the array index. |
| R12 | `grep -n "export async function" features/admin/product-image.ts` lists `requestImageUpload`, `attachProductImage`, `addProductImage`, `promoteProductImage`, `removeProductImage`, `reorderProductImages` — six total, each preceded by its own `requireVendorRole("ADMIN")` call in its body. `git diff` confirms `attachProductImage`'s and `setPrimaryProductImage`'s bodies are unchanged from `origin/staging`. |
| R13 | `grep -n "productImageSelect\|getProductForAdmin" -A 20 lib/repositories/products.ts` shows the select used by `getProductForAdmin` includes `id: true` and `sortOrder: true`. `grep -n "images" app/(admin)/staff/products/[id]/page.tsx` shows those fields passed through to `ProductForm`. |
| R14 | Read `components/staff/ProductForm.tsx`'s Images section: each rendered image has a set-primary control conditional on `!image.isPrimary`, a remove control, and up/down controls conditional on position; a distinct "add another image" upload UI exists alongside (not replacing) the existing uploader. |
| R15 | `npm run preview`, live, on a product that starts with exactly one image: (1) use "Add another image" with a second file — confirm the edit page now lists two images, and note the second image's `storageKey` (visible in its public URL); (2) click "Set primary" on the new one — confirm the storefront product page's primary image changed to the new upload; (3) click "Remove" on the demoted (now non-primary) image — confirm it's gone from the edit page, and confirm its object is deleted from storage: write a scratch file (e.g. in the repo's scratchpad, NOT committed) containing `import "dotenv/config"; import { getStorage } from "<repo-root>/lib/storage"; getStorage().headObject(process.argv[2]).then(console.log);`, run `npx tsx <that file> <removed-key>` from the repo root (so it picks up `.env`, matching the same Neon/S3 project `npm run preview` used — verify `.dev.vars`/`.env` agree first, per `CLAUDE.md`), and confirm it prints `null`; (4) add a third image, then use the reorder controls and confirm both the admin edit page and the live storefront product page (`ProductImageGallery`) show the new order. Record the exact product/keys used in `build-notes.md`. |
| R16 | `grep -n 'deleteObject' specs/architecture.md specs/decisions/ADR-003-storage-abstraction.md` shows both files describing it as a real method; `git diff origin/staging -- specs/architecture.md specs/decisions/ADR-003-storage-abstraction.md` shows both `version` front-matter lines changed. |
| R17 | `grep -n 'GAP-013\|GAP-014\|GAP-015' docs/gap-register.md` — each row's `Status` cell reads `Fixed`, and each cited detailed section names a real file/function from this slice (not "see #208"/"see #173" alone). |
| R18 | `grep -n '211\|catalogue-debt-bucket' specs/roadmap.md` shows a change-log row; `git diff origin/staging -- specs/roadmap.md` shows the `version` line changed. |
| R19 | `git diff --name-only origin/staging...HEAD | grep -qx 'CHANGELOG.md'` succeeds, and the `[Unreleased]` entry describes this slice. |
| R20 | `npm run lint`, `npm run typecheck` (`npx tsc --noEmit`), `npm test` (`npx vitest run`), `npm run format:check` each exit 0. On a Windows checkout, treat a `format:check` failure on files this slice did not touch as the documented `core.autocrlf` artifact — confirm via `git show HEAD:<file>` written with LF endings and `prettier --config .prettierrc.json --check`, and defer to CI as the authority. |
