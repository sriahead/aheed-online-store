# P8.1a — Frontend & Accessibility Debt (validation)

| Req | How to verify |
|-----|---------------|
| R1  | `npm run preview`, request `/orders/lookup` with a valid order-number/email pair (or read the rendered output directly). Confirm via `grep -n "<h1\|<h2\|<h3\|<h4" ` on the rendered HTML (or the source JSX) that the sequence is exactly: one `h1`, then `h2` (sr-only, "Delivery Status"), then three `h3` (step labels), then `h2` (sr-only, "Order Items"), then `h3` ("Items Ordered") — no `h4` anywhere on the page. |
| R2  | `npm run preview`, request `/staff/reports` signed in as `demo-admin@example.com`. Confirm the rendered `<h1>` text is exactly "Store reports", and that a `<h2>Sales</h2>` precedes three `<h3>` tiles (Total Revenue / Total Orders / Avg Basket Value) — `grep -n "<h1\|<h2\|<h3"` on the response, or read `app/(admin)/staff/reports/page.tsx` directly against R2's wording. |
| R3  | `grep -n "prisma" lib/repositories/loyalty.ts` inside `saveLoyaltySettings`'s body (line range) shows zero matches; `grep -n "getPrismaWs" lib/repositories/loyalty.ts` still shows it called inside that function. |
| R4  | `grep -n "flex items-center gap-3 shrink-0" components/layout/Header.tsx` shows `h-10` and `overflow-clip` present on that className, and `grep -n "overflow-clip" components/layout/Header.tsx` shows exactly one match (confirms it did not also land on the row or `<nav>`). Then `npm run preview`, sign in as `demo-admin@example.com`, open the `ViewSwitcher` control in the header, and confirm its dropdown still renders fully visible below the header row (not clipped) — this is the regression this scope change exists to prevent. |
| R5  | Read `lib/vendor-theme.ts`'s `brandStyle()`: confirm the `action`/`accent`/`danger` `clampForContrast` calls each pass a background array containing their matching tint (`p["green-tint"]`/`p["orange-tint"]`/`p["red-tint"]`) alongside `surfaces`. |
| R6  | Write a one-off `tsx` script (repo root, deleted after) importing `brandStyle`, `AHEED_PRIMITIVES`, `SRIMART_PRIMITIVES` and `contrastRatio`, printing `contrastRatio(style["--color-danger"], primitives["red-tint"])` etc. for both vendors — confirm all six values (3 pairings × 2 vendors) are `>= 4.5`. (Real DB not required — `brandStyle()` is pure.) |
| R7  | `npx vitest run tests/vendor-theme.test.ts` passes, and the file's diff shows a new test asserting R6's three pairings for both vendors (not just the manual script from R6 — a real, permanent regression test). |
| R8  | `git diff CHANGELOG.md` (against the branch's base) is non-empty and describes this slice. |
| R9  | `npm run lint`, `npm run typecheck`, `npm test`, `npm run format:check` all exit 0. |
