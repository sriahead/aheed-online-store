# ADR-004 slice 4 — branding-as-CSS-vars + config split (validation)

| Req | How to verify |
|-----|---------------|
| R1  | With `SEED_AHEED_HOST` set, run `npm run db:seed` against a scratch DB; query `SELECT "brandGreenDark","brandGreen","logoStorageKey" FROM "VendorBranding"`, `SELECT "localityName" FROM "VendorConfig"`, `SELECT "prefix" FROM "VendorDeliveryArea"` for the Aheed vendor — values match R1 (green-dark `#1b5e20`, logo key `vendors/{AHEED_VENDOR_ID}/logo.png`, locality "Milton Keynes", prefix "MK"). Re-run `db:seed`; row counts unchanged (idempotent). |
| R2  | With **both** `SEED_AHEED_HOST` and `SEED_SRIMART_HOST` set, re-seed; SriMart's `VendorBranding` hex differ from Aheed's, its `localityName` and `VendorDeliveryArea.prefix` differ, and its `logoStorageKey` is NULL. With only `SEED_AHEED_HOST` set, no SriMart branding/config rows are written. |
| R3  | `npm run lint` exits 0 (the no-direct-Prisma ESLint guard passes); `grep -rn "@prisma/client\|@/lib/db" app components lib/delivery.ts` shows no new import outside `lib/repositories/*`. `lib/repositories/vendor.ts` wraps its lookups in React `cache()`. |
| R4  | Read `app/(storefront)/layout.tsx`: the wrapper element's `style` sets all eight `--color-brand-*` properties from the branding read path. `git diff design-system/tokens/tokens.css` is empty (tokens unchanged). `npm run preview`, request Aheed's host, and confirm in the served HTML the wrapper carries `--color-brand-green-dark:#1b5e20` (Aheed) and, on SriMart's host, SriMart's hex. |
| R5  | `grep -n "Aheed Food Centre\|Milton Keynes" components/layout/Header.tsx` returns nothing; the promo/delivery strings interpolate `name`/`localityName`. |
| R6  | Read `components/layout/Header.tsx`: `<img>` is rendered only when `logoStorageKey` && `CDN_BASE_URL`, else a text wordmark. On staging (Aheed logo uploaded + `CDN_BASE_URL` set) the Aheed header serves the logo image (browser DevTools shows a 200 for `${CDN_BASE_URL}/vendors/{AHEED_VENDOR_ID}/logo.png`); SriMart's header shows the wordmark text. |
| R7  | `npm test` includes `isDeliverable` cases; the four assertions in R7 pass. `grep -n "MK\[" lib/delivery.ts` shows the hardcoded MK regex is gone (prefixes are a parameter). |
| R8  | `grep -n "Milton Keynes\|MK postcodes\|\"MK\"" app/(storefront)/page.tsx` returns nothing; `npm run preview` on Aheed's host → hero shows the Aheed tagline and the postcode checker rejects a non-MK code with a "Milton Keynes … postcodes only" message; on SriMart's host the locality/prefix in the message are SriMart's. |
| R9  | `grep -n "Aheed Food Centre — Milton Keynes" app/layout.tsx app/(storefront)/page.tsx` returns nothing; both files export `generateMetadata`. `npm run preview`: view-source on Aheed's homepage `<title>` reflects Aheed branding; `/coming-soon` (no vendor) renders the neutral fallback title without error. |
| R10 | Read `lib/auth.ts`: reset/verify subject strings are built from a resolved `senderName` with a default fallback. A unit test (or a `preview` run triggering a reset with logging) shows the subject carries the vendor sender name; the From value is unchanged from before the slice. |
| R11 | `npm test` exits 0 with the new `isDeliverable` and branding-read-path tests present and passing. |
| R12 | On the seeded staging DB, load `https://staging.aheedfoodcentre.nocaped.com` and `https://srimart-staging.nocaped.com` in the browser: header/name/logo-or-wordmark, primary colours, and delivery locality differ between the two, driven only by data. |
| R13 | `specs/design-system.md` and `specs/architecture.md` contain the slice-4 notes with bumped `version:` front-matter; `docs/env-setup.md` documents the Aheed logo upload; `npm run kms:validate` exits 0; `ARTIFACT_INDEX.md` regenerated (its build line updated) and lists this slice's `plan.md`. |
| R14 | `git diff origin/staging -- CHANGELOG.md` shows a new `[Unreleased]` entry referencing #73. |
| R15 | `npm run lint`, `npm run typecheck`, `npm test`, `npm run format:check`, `npm run kms:validate` all exit 0 (trusting CI `gates` as the authority over local Windows CRLF noise per the workflow). |
