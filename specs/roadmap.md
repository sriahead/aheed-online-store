---
id: roadmap
title: Roadmap
audience: [dev]
type: doc
status: approved
version: "1.17.0"
updated: 2026-08-12
visibility: internal
summary: Master backlog and phase sequencing (M0, P0-P8, plus inserted P2.5) for the Aheed Online Store, plus the running change log of roadmap revisions and phase closures.
tags: [roadmap, phases, backlog]
---

# Roadmap

Master backlog for the Aheed Online Store (SDD Solo Mode). Phases deliver working software early
and minimise risk. The pivot to Cloudflare Workers + Neon adds a **Milestone 0** before P0:
prove the infrastructure end-to-end with a walking skeleton, *then* build features.

> **Delivery tracking.** The GitHub Project *Aheed Online Store — Delivery* is a generated **view** of
> this roadmap, never a second plan. It holds only the **status layer** — what is in progress, in
> review, or blocked right now. Phases, scope and acceptance criteria live here and in
> `specs/<slice>/`; if something belongs in a doc, it does not go in the Project.
> Provisioned by `scripts/provision-project.sh` (idempotent).

## Milestone 0 — Walking Skeleton (infrastructure proof) 🩻
The smallest app that exercises `browser → Worker → Prisma → Neon` on real infra, plus the full
CI/CD path (auto staging deploy, manual-approved production deploy). One `HealthCheck` model, a
landing page, and `/api/health`. **Exit criteria:** production `/api/health` returns `db.ok: true`
and the manual approval gate works. Spec: `specs/2026-08-05-m0-walking-skeleton/`. Runbook:
`docs/walking-skeleton-runbook.md`. No features, no auth, no design system — deliberately.

> Once M0 is green, the roadmap proceeds through P0–P8 below, each behind its spec and the four gates.

## Phases

- **P0 — Foundation & scaffolding.** Harden what M0 proved: tooling (ESLint/Prettier/Vitest/Husky),
  SDD git hooks, base app shell, `lib/config`/`lib/db`/`lib/storage` finalised, `public/` SEO/PWA
  surface, design-system tokens. Delivers a deployable, gated app skeleton.
- **P1 — Auth & accounts.** Better Auth: email/password + Google Sign-In, session/refresh, RBAC
  (Customer/Staff/Admin). Account area shell. Guest browsing.
- **P2 — Catalogue & browsing.** Categories, product pages (image via storage port + CDN using
  relative keys), search & filters, keyset pagination.
- **P2.5 — Ratings, reviews & storefront visual design.** Inserted after P2's close, not part of
  the original plan — the human's AI Studio design mockup (`aheedfoodcentre.ai.studio`) set an
  expectation for storefront polish and product ratings/reviews that P0–P2 deliberately deferred
  in favour of infrastructure first. Two slices: (a) real `Review` model + submission (gated to
  authenticated users, reusing P1's auth) + rating aggregation — data before display, so the
  visual pass consumes real numbers from day one, not placeholders revisited later; (b)
  storefront visual redesign (header/promo bar, hero, category sidebar, redesigned product
  cards/detail) matching the mockup's look, plus small schema additions (`origin`, discount
  pricing) the mockup's cards need. Explicitly excludes cart/"Add to Cart" wiring — that's P3,
  unchanged by this insertion.
- **P3 — Cart & checkout.** Cart (making the currently-inert "Add to Cart" real), guest + account
  checkout, Stripe payment, atomic order creation with stock decrement, on-screen + emailed
  confirmation. Also includes **"Shop your list"** (paste/build a list → match to catalogue → add
  to cart) — folded in here rather than a separate feature, since it's a cart-entry path.
- **P4 — Orders & delivery status.** Order history, three-step status with audit trail, staff
  updates, confirmation/delivery emails.
- **P5 — Loyalty & discounts.** Points earn/redeem; discounts engine; admin configuration.
- **P6 — Admin & staff panel.** Product/category management, availability, order dashboard,
  customer directory, discounts, loyalty rules, reports.
- **P7 — Compliance & hardening.** UK GDPR, PECR consent, T&Cs/Privacy, accessibility, OWASP
  hardening, backups + monitoring, index/query review vs NFR targets.
- **P8 — Deployment & launch.** Full Cloudflare + Neon + R2 wiring, secrets, backups/PITR,
  monitoring, UAT, production deployment, handover & training. Migration playbooks verified.

## Roadmap Change Log

| Date | Change | Reason |
|------------|-------------------------------|--------------------------------|
| _pending_  | Baseline set                  | To be recorded at Phase 3 freeze |
| 2026-08-05 | Architecture pivot to Cloudflare Workers + Neon + S3-compatible storage | PostgreSQL-first, vendor-agnostic, cost-effective mandate (supersedes GCP-origin ADR-001) |
| 2026-08-05 | Added **Milestone 0 — Walking Skeleton** before P0 | De-risk infra end-to-end before feature work |
| 2026-08-06 | **Milestone 0 closed.** `/api/health` returns `db.ok: true` on both staging and production; `gates`/`deploy-staging`/`deploy-production` all green. Infra learnings (Prisma driver-adapter config, Workers `fs` polyfill gaps, Next 16/Turbopack, CI secrets/environments) captured in `CLAUDE.md` and `CHANGELOG.md` rather than a separate retro. Known gap: no enforced required-reviewer gate on `production` (needs a paid GitHub plan). | Exit criteria met; proceeding to P0 |
| 2026-08-06 | **P0 closed** (backfilled retroactively — never logged here at the time). Foundation & scaffolding: SDD git hooks, Prettier, base app shell, `lib/config`/`lib/db`/`lib/storage` finalized, design-system tokens (Tailwind v4 + Aheed brand kit), `public/` SEO/PWA surface. | Exit criteria met; proceeding to P1 |
| 2026-08-07 | **P1 closed** (backfilled retroactively). Better Auth email/password + Google Sign-In, RBAC (Customer/Staff/Admin), account shell, guest browsing — split into P1a/P1b since Google Sign-In needed an OAuth client only the human could provision. | Exit criteria met; proceeding to P2 |
| 2026-08-07 | **P2 closed.** Categories, product pages, images via the S3-compatible storage port + CDN, keyset pagination, global search, price/availability filters — split into P2a/P2b so search/filters could be validated separately from core browsing. Real bugs caught and fixed while shipping: `next build` static-optimizing a Prisma-backed page (same root cause as P1b's `/login`/`/register` fix, now hit a second time — worth remembering as a pattern, not a one-off), a Cloudflare Security Level setting blocking CDN image serving on both staging and production zones, and a seed-script idempotency bug from a partial mid-run failure. Trigram/fuzzy search and a dedicated search service remain explicitly deferred until the catalogue actually grows past its current placeholder data. | Exit criteria met; proceeding to P2.5 |
| 2026-08-07 | **Added P2.5 — Ratings, reviews & storefront visual design**, inserted between P2 and P3. Not part of the original plan — the human's AI Studio design mockup (`aheedfoodcentre.ai.studio`) surfaced two gaps: storefront visual polish deferred in favour of infra-first delivery, and product ratings/reviews, never in the original P0–P8 scope at all. Two slices, reviews backend before the visual pass (real data before display). Explicitly excludes cart/"Add to Cart" wiring, unchanged as P3's scope. | Confirmed with the human; proceeding to P2.5a |
| 2026-08-07 | Identified a **new, unscheduled future item**: a "Developer Control Toolbar" (role/view switching, infra introspection), surfaced by the mockup's real exported source. Confirmed as intended real scope, not just demo navigation — tracked as issue #41, deliberately not placed in the phase sequence yet (needs its own `/propose`; its "Dev KMS" concept as shown conflicts with `CLAUDE.md`'s secrets posture if taken literally). | Tracked, unscheduled — not blocking P2.5b |
| 2026-08-07 | **P2.5 closed.** Ratings & reviews (auth-gated submission + denormalized aggregates) and the storefront visual redesign matching the AI Studio mockup — real header/hero/category-sidebar/redesigned cards, speciality (Halal/Fresh/Organic) filters, a postcode deliverability checker, and a two-layer token/schema/seed foundation underneath. Split into three slices (P2.5a reviews backend, P2.5b1 visual foundation, P2.5b2 UI) so each stayed independently validatable — the split kept catching real bugs (a seed idempotency gate that would have skipped every new category; rating fields P2.5a denormalized *for the cards* but no read path ever exposed until P2.5b2; `docs/ui-ref/` never excluded from lint/prettier/tsc). Cart/"Add to Cart" wiring stays inert by design — P3, unchanged. Deferred to their own issues: a homepage featured-products rail (#45, no backing data yet), `next/image` adoption (#46), and the Dev Control Toolbar (#41). | Exit criteria met; proceeding to P3 |
| 2026-08-07 | **Post-P2.5 storefront polish** shipped as fixes from live review (not a phase): Milton Keynes delivery area broadened to any MK district, real cropped logo, mobile-UI rework, and a browse-page layout flip — departments to a horizontal arrow-scrolled strip, search/filters to a vertical left sidebar (`DepartmentScroller` is the first storefront client component). **Multi-tenancy** flagged as a pre-P3 foundation (ADR-004 draft, #49) with an env-config CLI + `docs/env-setup.md` alongside. | Live-review feedback; not a roadmap phase |
| 2026-08-07 | **P3 scope confirmed/expanded:** the cart becomes real in P3 (the inert "Add to Cart" gets wired), and **"Shop your list"** is folded into P3 as a cart-entry path rather than a standalone feature — keeping the shopping flow in one phase. The **Developer Control Toolbar / dev view (#41)** is being pulled forward next as its own propose→spec→build. | Confirmed with the human |
| 2026-08-08 | **ADR-004 (multi-tenancy) Accepted** (`specs/decisions/ADR-004-multi-tenancy.md`, issue #49, v1.0.0). Four decisions locked: subdomain resolution + optional custom-domain override; row-level `vendorId` enforced in the repository layer; global identity with family-scoped SSO / isolated session per custom domain (authz via `VendorMembership`); separate the shared staging/prod Neon DB into two projects **first**. Sequenced as slices 0–4 to land **before P3**, since orders/carts/payments would otherwise bake in single-vendor assumptions across every repository query. Dev view (#41) is now implemented. | Decisions signed off with the human |
| 2026-08-08 | **ADR-004 slices 0–1 shipped to production.** Slice 0 (#56): staging/prod split into **separate Neon projects** (env isolation before tenant work). Slice 1 (#62): `Vendor` aggregate + empty `VendorBranding`/`VendorConfig`/`VendorDeliveryArea` satellites, required `vendorId` on Category/Product/Inventory/Review with per-vendor composite uniques and vendorId-leading indexes; a hand-authored migration backfilled all existing rows to a single Aheed vendor (applied cleanly on real prod data). Also shipped alongside: the standalone demo-accounts tool (#57) and the KMS internal docs site going live behind Cloudflare Access (#61). Deferred follow-ups tracked in #65 (roadmap note done here; migration-drift check pending). | Foundation landing before P3 |
| 2026-08-08 | **ADR-004 slice 2** (#66): repository-layer `vendorId` enforcement — a `getCurrentVendorId()` tenant seam (interim single-vendor; slice 3 swaps in host resolution) with `where: { vendorId }` injected across `lib/repositories/*`, plus an ESLint guard forbidding direct Prisma access in the app/UI/feature layers. Runtime output unchanged at one vendor. Slice 3 (host→tenant resolver + auth) and slice 4 (branding/delivery/config wiring) remain. | Proceeding through the ADR-004 slice sequence |
| 2026-08-08 | **ADR-004 slice 3a** (#68): per-vendor authorization — `VendorMembership(userId, vendorId, role)` splits *platform* role (`User.role`, `/dev` gate) from *per-vendor* staff/admin; `requireVendorRole()` allows platform admins or matching members. Additive migration. **Slice 3b** (#70): host→tenant resolution — a `VendorDomain(host)` table + `lib/tenant.ts` resolver serve the right vendor by request host (unmatched → `/coming-soon`; no Next middleware, edge is forbidden); **SriMart** added as a real 2nd vendor with its own catalogue + domains, proving isolation. Additive migration. | Proceeding through the ADR-004 slice sequence |
| 2026-08-08 | **ADR-004 slice 4** (#73) + follow-ups: data-driven vendor **branding/config/delivery** — the eight `--color-brand-*` primitives injected per request, header/hero copy, locality, delivery area, logo, metadata and email sender from the DB; SriMart seeded with a distinct blue/tech palette + Reading locality. Follow-ups from live review: per-vendor **theme** applied to *semantic* tokens not just primitives (#77), per-vendor **search placeholder** (#78, additive `VendorConfig.searchPlaceholder`), and browse/product polish — data-driven speciality filters, brand-neutral product placeholder, vendor-aware manifest (#79/#80). | Two hosts render as different vendors from data alone |
| 2026-08-09 | **ADR-004 slices 3b + 4 promoted to production** (PR #81): `staging → main` promotion; `deploy-production` green (additive `searchPlaceholder` migration applied). Per-vendor search placeholder + neutral product placeholder need a prod re-seed to surface. | Shipped foundation to production before continuing |
| 2026-08-09 | **ADR-004 slice 3c** (#74): **data-driven auth cookie scoping** — the last multi-tenancy gate before P3. `getAuth()`'s `baseURL`/`trustedOrigins`/cookie domain resolve per request from the host alone (`lib/auth-origin.ts`, no DB call); **host-only sessions trusting only that vendor's own origin by default** (no shared subdomain family exists — Aheed and SriMart are on distinct hosts), with a config-gated `AUTH_COOKIE_FAMILY_DOMAIN` (unset today) that arms future parent-domain family SSO with no code change. ADR-004 breadcrumb records the topology-vs-decision-4 reconciliation. **Correction (#83), caught live on staging right after merge:** an earlier draft populated `trustedOrigins` from every `VendorDomain` host, which would have let one vendor's origin pass another vendor's auth CSRF check — narrowed to same-vendor-only and verified live (same-origin accepted, sibling-vendor origin rejected identically to unknown). **ADR-004 slice sequence complete — P3 (cart & checkout) unblocked.** | Auth made per-vendor before P3 adds order/checkout query surface |
| 2026-08-09 | **P3a — cart foundation** (#93, `specs/2026-08-09-p3a-cart-foundation/`): the storefront's inert "Add to Cart" became real. Vendor-scoped `Cart`/`CartItem` behind `lib/repositories/cart.ts` — one shopper gets an **independent cart per vendor**; identity is **exactly one of** `userId` or an opaque `guestToken` in a host-only `aheed_cart` cookie (mirroring slice 3c's isolation), created **lazily** so no row and no cookie exist until something is added. Cart actions split one-file-per-action during validation, correcting a consolidated `actions.ts` that had deviated from the spec. | First P3 slice; cart before checkout can consume it |
| 2026-08-10 | **P3b — checkout + order core** (#96, `specs/2026-08-10-p3b-checkout-order-core/`): a cart can become a real order. Vendor-scoped `Address`/`Order`/`OrderItem`/`Payment`/`OrderStatusEvent` behind `lib/repositories/orders.ts`, a `/checkout` page and a `/checkout/{orderNumber}` confirmation served from the persisted order. **Overselling is structurally impossible** — the whole checkout is one interactive transaction whose stock decrement is a conditional `updateMany` (`quantity: { gte: qty }`), and clearing the cart inside it makes a double submit safe. Money is recomputed server-side and snapshotted onto `OrderItem`; the delivery address is snapshotted per order. Order numbers are deliberately **non-sequential** (a counter would leak order volume). `PaymentService` port created with a **stub**, so the risky logic was testable before any Stripe credential existed. **ADR-005 (payments & multi-vendor money flow) accepted** alongside. Recorded a deliberate gap: an abandoned checkout holds its stock until P3c's webhook releases it, so **P3b must not reach production ahead of P3c**. | Order core before payments, so the money path had something correct to attach to |
| 2026-08-10 | **P3c — Stripe payments, webhooks & confirmation email** (#99, `specs/2026-08-10-p3c-stripe-payments/`): money actually moves. Real hosted **Stripe Checkout** via raw `fetch` (no SDK — same Worker-bundle reasoning as `aws4fetch`), a signature-verified (WebCrypto HMAC over the **raw** body) idempotent webhook at `/api/webhooks/stripe`, and a confirmation email sent **only** after payment confirms. **Closes P3b's stock-release gap** — a failed or expired order returns its items to stock. Also fixed a latent P3b defect: `createPayment()` ran *inside* the Prisma transaction, harmless with a stub but a real HTTP call there would hold a Postgres transaction open against a 5s timeout; the session is now created after commit, with a compensating transaction if it fails. **Live-verified on staging with real test-mode keys** (two real payments). Two gaps left tracked, both infra not code: Resend has no verified sending domain, so no confirmation email can reach a real inbox in any environment (**#104**, owner action), and the payment-provider failure path needs a deliberate window against staging's live secrets (**#103**). | Completes the paid-order path; P3 stays open pending P3d |
| 2026-08-10 | **P3a + P3b + P3c promoted to production** (PR #108): `staging → main`; `deploy-production` green, production `/api/health` reporting commit `034a380` with `db.ok: true`, and `/api/webhooks/stripe` live (an unsigned POST is rejected 400, so signature verification is active). The two P3 migrations were already applied to the production database, so CI reported "no pending migrations" — they had been applied out-of-band rather than by a `deploy-production` run, since no `main` push occurred between PR #81 and this one. No drift resulted (CI confirming zero pending *is* the proof repo and DB agree), but migrations are supposed to run in CI per `CLAUDE.md`. Shipped deliberately **before the storefront is open to customers**: production runs Stripe **test-mode** keys, so no real money can move, and a live key is a separate decision (#113). Also promoted alongside: the SDD workflow v2 loop and its machine checks (#106). | P3's paid-order path reaches production; storefront stays closed until live keys + #104 |
| 2026-08-10 | **P3 remains open — P3d ("Shop your list") is the last slice.** P3a/P3b/P3c delivered cart → checkout → payment, but P3's scope also folds in "Shop your list" (paste/build a list → match to catalogue → add to cart) as a cart-entry path, recorded as deferred in P3c's plan. **These three rows were backfilled on 2026-08-10**, having been missed at the time — the gap that motivated the post-Ship documentation audit in `specs/sdd-workflow.md` 2.0.0. | Backfill; P3 not yet closable |
| 2026-08-10 | **P3d — "Shop your list"** (#114, `specs/2026-08-10-p3d-shop-your-list/`): the second way to fill a cart, and P3's last slice. Paste a list → each line parsed for a quantity → matched against **this vendor's** catalogue by token-AND on product `name` → a **mandatory review step** → confirmed lines added in one transaction. All the thinking is pure (`lib/shopping-list.ts`, 22 tests, no DB); `ProductRepository.matchListTerms()` issues **one** query for the whole list (candidates capped at 200), so a 100-line paste is not 100 queries. Deliberately not `search()`, which ORs a single `contains` across name and description and so matches nothing for `"2x chicken breast"`. **The matching pass writes nothing at all** — no `Cart`, no `CartItem`, no guest cookie — preserving P3a's rule that browsing creates no state; the guest token is issued only on the add. Review-form product ids are **untrusted**, defended by the existing vendor-scoped `stockMap` rather than a re-implemented check. No schema change, no migration. Validated live against real Postgres on `npm run preview`, all 25 rows, none unverified. Typo/fuzzy matching stays out (needs `pg_trgm` + `$queryRaw`, forbidden in application code); two follow-ups filed: partial-match fallback for a real product name plus an unknown size (**#115**) and saved reusable lists (**#116**). | Completes P3's cart-entry paths |
| 2026-08-10 | **P3 closed.** Cart → checkout → payment → list-based cart entry all shipped to production (P3a #93, P3b #96, P3c #99, P3d #114). The inert "Add to Cart" from P2.5 is real, guest and account carts merge on sign-in, orders are created atomically with stock decrement, Stripe hosted Checkout settles via a signature-verified idempotent webhook, and a shopper can fill a cart by pasting a list instead of clicking. **Two known gaps carried out of the phase, both infra not code:** Resend has no verified sending domain, so no confirmation email reaches a real inbox in any environment (**#104**, owner action), and the payment-provider failure path still needs a deliberate window against staging's live secrets (**#103**). Neither blocks P4. | Exit criteria met; proceeding to P4 |
| 2026-08-11 | **P4a — order history & status timeline** (#122, `specs/2026-08-11-p4a-order-history/`): the first P4 slice and the **read half** of the phase. `/account/orders` lists the signed-in shopper's own orders for **this vendor**, newest first, ten per page (keyset on `(createdAt, id)`, never `OFFSET`); `/account/orders/{orderNumber}` shows the snapshotted items, the snapshotted address, the money breakdown and a **status timeline**. P4a built far less than P4's roadmap line implied, because P3 left more behind than the line recorded — `OrderStatusEvent` rows were already written in three places and `Order` already carried `@@index([vendorId, createdAt])` — so the slice is **read-only: no transition, no cart write, no schema change, no migration**. **`getForUser()` is a second, stricter read beside an untouched `getByOrderNumber()`:** the existing method implements P3b's capability-URL rule (a guest order has no owner, so the unguessable number *is* the credential), which is right for `/checkout/{n}` and wrong for `/account/orders/{n}`; `userId` sits in the `WHERE`, so a guest order and another member's order both simply fail to match. **The timeline is built from `status` and structurally cannot render `OrderStatusEvent.note`** — P4b hands staff a control that writes that column, and an internal note on a customer's own order page would be a live incident, so the types make the leak unrepresentable rather than merely unlikely. The list is deliberately **unfiltered by status**: an abandoned `PENDING_PAYMENT` order is the one a shopper is most likely hunting for. `components/orders/` extracts the items/totals and address cards out of the P3b confirmation page verbatim, so both pages share one implementation. Validated live against real Postgres on `npm run preview`, all 30 rows, **none unverified** — including an empirically measured constant query count (2 at `take=1` and at `take=10`, i.e. no N+1) and a note smuggled into the DB by hand that never reached the page. Deferred with issues filed: guest order lookup (**#123**, a security decision on credentials/rate-limiting/enumeration), reorder-from-a-past-order (**#124**, a cart write), and all transitions + delivery emails (**#125**, P4b). | First P4 slice; the read path before P4b adds writes |
| 2026-08-11 | **P4a promoted to production** (PR #128): `staging → main`; `gates` green on the promotion, `deploy-production` and `deploy-docs-internal` both green at `d9f968f`. The slice touched no `prisma/` path, so unlike the P3 promotions this one carried **no migration risk** and CI had nothing to apply. **This row was backfilled on 2026-08-11**, on P4b's branch under the carry-forward rule — every P3 promotion got its own row (PRs #81, #108, #121) and P4a's did not. `sdd:audit` did not catch it: that check asks whether a roadmap row *cites the slice path*, which the P4a row does, so a missing **promotion** row sits one layer below what it can see. Worth knowing before trusting a clean audit as proof the record is complete. | Carry-forward doc correction identified at P4b's Orient |
| 2026-08-11 | **P4b — staff order status transitions & delivery emails** (#125, `specs/2026-08-11-p4b-order-status-transitions/`): the write half of P4. `OUT_FOR_DELIVERY` and `DELIVERED` had been in the `OrderStatus` enum since P3b's initial migration with **nothing able to reach them** — an order landed on `CONFIRMED` when Stripe's webhook fired and stayed there forever. A vendor-role-gated queue at `/staff/orders` (a new `/staff` segment parallel to `/dev`, giving P6 a namespace to grow into) advances orders one rung at a time, and is the **first real consumer of ADR-004 slice 3a's `VendorMembership`**. **A double-advance is structurally impossible:** legality is evaluated against the *persisted* status and the write is a conditional `updateMany` repeating that status — P3b's anti-overselling compare-and-set aimed at a different race — with `vendorId` in the same `where`, so another vendor's order is indistinguishable from one that doesn't exist. One additive migration: **`OrderStatusEvent.createdByUserId`**, nullable, `ON DELETE SET NULL`, which **corrected #125's own "no schema change" line** — an Orient-time observation, not a decision, that could not survive contact with "the acting user recorded"; P4's line here promises an audit trail, and attribution not captured now is unbackfillable. Transition rules are pure and unit-tested across the full 5×5 matrix by asserting the *exact* legal set, so a future status cannot widen the ladder silently. The server action re-runs its own RBAC check, because a server action is a public endpoint at a stable id. Emails on the two staff transitions only, after commit, never throwing. **`note` stays system-written** — no staff free-text field, so P4a's unrenderable-note guard is honoured by not opening the door. Staff cancellation and refunds stay out (ADR-005 territory); a staff order **detail** view, filters, search and bulk actions deferred to P6 (**#129**). | Completes P4's status ladder; phase closes on promotion |
| 2026-08-11 | **P4b promoted to production** (PR #131): `staging → main`; `gates` green on the promotion, `deploy-production` and `deploy-docs-internal` both green. **Unlike P4a's promotion this one carried a migration** — `deploy-production` is the run that actually applied `OrderStatusEvent.createdByUserId` to production's database, so it was the row to watch rather than the merge. Purely additive (one nullable column + its FK, no `DROP`, no `SET NOT NULL`, no `ALTER TYPE`), so no backfill and no window where the running app was inconsistent with the schema. **This row supersedes `build-notes.md`'s "nothing in this slice has touched a real database"**, which was true when written and is what pointed validation at the right places: every DB-touching row was then verified live, and three of the four "known-shaky" areas the notes named came back clean — the compare-and-set survived a genuine concurrent `Promise.all` double-submit (exactly one winner, exactly one event row) rather than only a hand-written Prisma double; the keyset crossed a real 20/6 page boundary with disjoint pages whose union exactly matched the DB's actionable set; and the migration applied with the `OrderStatusEvent` count unchanged at 28, every pre-existing row null, no drift. R24's two-legitimate-outcomes ambiguity resolved to the **200-with-staff-only-refusal** branch (the validation staff account holds no SriMart membership), recorded here because the row asked which. | Promotion of #130; P4b in production |
| 2026-08-11 | **P4 closed.** Order history, the three-step status ladder with an attributed audit trail, staff updates and delivery emails all shipped to production (P4a #122, P4b #125). An order's status now *moves*: `OUT_FOR_DELIVERY` and `DELIVERED` sat unreachable in the enum from P3b's first migration until this phase, and the customer can see every step on their own order page while the staff member who caused it is on the record. **One known gap carried out of the phase, infra not code and inherited from P3:** Resend still has no verified sending domain (**#104**, owner action), so P4b's delivery emails are correct in every structurally checkable respect — right recipient, right per-vendor sender identity, attempted exactly once per transition, never throwing — and still cannot be confirmed to reach a real inbox in any environment. **Deliberately out of scope and tracked, not remembered:** guest order lookup (**#123**), reorder-from-a-past-order (**#124**), and the staff order *detail* view with filters, search and bulk actions (**#129**), which P6's admin panel supersedes wholesale rather than extends. Staff cancellation and refunds remain ADR-005 territory. Neither blocks P5. | Exit criteria met; proceeding to P5 |
| 2026-08-11 | **P5a — loyalty points: earn, redeem, tiers, expiry & admin config** (#135, `specs/2026-08-11-p5a-loyalty-points/`): the first P5 slice and the **loyalty half** of the phase; the discounts engine stays P5b. A shopper earns per-vendor points on orders they actually pay for, spends them at checkout, and loses them to inactivity. **The whole design rests on one seam:** `computeTotals` is the only place an order's money is decided, so adding a discount *there* meant `lib/payments.ts` and the Stripe webhook needed **no change at all** — by the time the payment path sees an amount, it is simply the amount. ADR-005 gains an additive implementation note; **no ADR-005 decision is reopened**. Free delivery and the vendor's `minimumOrderPence` are both judged on the subtotal **before** the discount, so spending points cannot claw back custom already earned. **A point cannot be spent twice:** `LoyaltyAccount.balancePoints` is a compare-and-set counter guarded by a conditional `updateMany` carrying `vendorId`, `userId`, `balancePoints: { gte: n }` and the lapse bound — P3b's anti-overselling technique and P4b's status advance, aimed at a third race — with the append-only `LoyaltyLedgerEntry` beside it exactly as `OrderItem` sits beside `Inventory.quantity`. **`LoyaltyAccount` is deliberately not a cache of the ledger**: a balance derived by `SUM()` cannot be compare-and-set, which is why the counter exists *alongside* its ledger rather than instead of it. The debit runs **before** the `Order` row is written, so an order can never carry a discount whose points the shopper turned out not to have; `@@unique([orderId, kind])` makes a second `EARN` (duplicate Stripe delivery) or a second `REDEEM` (double submit) a database error rather than a check someone must remember. **Expiry is derived at read time and added no infrastructure** — `wrangler.toml` still declares no cron triggers; a lapsed balance reads as zero, is refused by the redemption guard, and is **reset rather than incremented** by the next earn, three behaviours that must stay in sync. Earning excludes both delivery and the discounted portion, so redeemed points cannot re-earn points — a slow value leak with no obvious symptom. Tier multipliers are basis points, **snapshotted onto the `EARN` row** so a historical earn stays explainable after the tier table changes underneath it. One additive migration (three tables, one enum, `Order.discountPence`, six `VendorConfig` columns, all defaulted); `Order.discountPence` is deliberately **generic, not points-specific**, so P5b's codes reduce the same total by the same arithmetic. Seed turns loyalty **on** for Aheed with two tiers and **off** for SriMart, which is what proves it is per-vendor data. The confirmation email gains a discount line — a correction forced by the slice, not a new message. **This row supersedes `build-notes.md`'s "nothing in this slice has touched a real database"**, honest when written and exactly what pointed validation at the right places: all 65 rows were then walked live against real Postgres, and **seven of the notes' eight "known-shaky" areas came back clean** — the concurrency guarantee survived a genuine `Promise.all` double-spend (one winner, one `REDEEM`, balance never negative) rather than only a mocked `count: 0`; both idempotency guards held against repeated `confirmPayment`/`failPayment`; the lapse/reset triangle set the balance to exactly the newly earned amount rather than stale+new; the structural `Tx` typing worked against Prisma's real interactive-transaction client; guest-vs-signed-in earning was checked *in the same run*, so the guest's zero proves something; and the admin form's positional tier pairing updated SILVER while leaving GOLD untouched. The eighth stands: **#104** still blocks any inbox-level email proof. **Validation also surfaced a real environment defect that had nothing to do with the slice:** `.env` *and* `.dev.vars` both pointed at the **production** Neon project while the same files' `S3_BUCKET`/`CDN_BASE_URL` correctly said staging, so P5a's migration was applied to production **before** its promotion PR — additive and provably harmless (`Order`/`OrderItem`/`OrderStatusEvent` counts unchanged, every row `discountPence = 0`, no drift), but the same slip against a destructive migration would not have been. Both files were repointed at staging and every live row re-run there. **Consequence for the promotion of this slice: `deploy-production` will report no pending migrations, and that is expected rather than drift** — the same situation PR #108 recorded for P3. This supersedes **#119**, whose framing (".env and .dev.vars point at *different* Neon projects") turns out to describe only half the failure mode: two files agreeing with each other proves nothing if both agree on the wrong target. `CLAUDE.md` now requires diffing both against `secrets/staging.vars` and `secrets/production.vars` rather than only against each other. Deferred with issues filed: points-earned messaging on confirmation/history pages (**#138**), earn reversal for cancelled/refunded confirmed orders (**#137**, unreachable today since `releaseOrder` acts only on `PENDING_PAYMENT`), and tier create/delete from the admin UI (**#136**, edit-only ships here). | First P5 slice; loyalty before the discounts engine, since its config extends an established `VendorConfig` shape rather than inventing an ad-hoc creation surface |
| 2026-08-11 | **P5a promoted to production** (PR #140): `staging → main`; merge `15c6c69`, `deploy-production` and `deploy-docs-internal` both green. Scope widened after the PR opened — it carried **#139** (the slice) *and* **#142** (the closeout docs), because #142 merged to `staging` while #140 was still open. **`prisma migrate deploy` reported no pending migrations, which is expected rather than drift:** P5a's migration had already reached production during validation via the `.env`/`.dev.vars` misconfiguration, and CI confirming zero pending *is* the proof repo and DB agree (same situation PR #108 logged for P3). **The feature is live but DARK** — `loyaltyEnabled` defaults false and production was never re-seeded, so `/account/loyalty` 404s for every shopper and no `EARN` row can be written (**#143**, an owner decision on live rates). **P5 is NOT closed by this promotion**: P5b (discounts engine) remains, and #143 must be resolved before the phase's shipped half is reachable at all. **This row was backfilled on P5b's branch under the carry-forward rule** — P5a's `/document` pass ran before its promotion, so 1.14.0 carried the slice row and no promotion row. `sdd:audit` did not catch it, for the second time: that check asks whether a roadmap row *cites the slice path*, which P5a's slice row does, so a missing **promotion** row sits one layer below what it can see (**#144**). | Carry-forward doc correction identified at P5b's Orient |
| 2026-08-11 | **P5b — discount codes: engine, checkout application & staff admin** (#145, `specs/2026-08-11-p5b-discount-codes/`): the second and final P5 slice, completing the discounts half P5a's loyalty half left open. A vendor admin creates a per-vendor code; a shopper enters it at checkout; it reduces `Order.discountPence` alongside any loyalty points, without a usage cap ever being exceeded. **P5a's generic `Order.discountPence` paid off exactly as designed**: `computeTotals` already took a discount parameter, so `lib/payments.ts`, the Stripe webhook and `Order` itself needed **no change at all**, and no ADR-005 decision reopened. **Codes and points stack, code first**: the code is evaluated against the true pre-discount subtotal (a percentage must not shrink because points were also spent), and `clampRedemption` gained an optional `existingDiscountPence` (default `0`, every P5a case unmodified) so points fill only the headroom the code left. **The usage counter counts DOWN** — `DiscountCode.remainingRedemptions`, nullable for unlimited — because `usedCount < maxRedemptions` is a column-to-column comparison Prisma cannot express in a `where` and raw SQL is forbidden in application code; counting down restores the same literal-to-column guard `Inventory.quantity` (P3b) and `balancePoints` (P5a) already use. **The per-customer cap is structural, not a count-then-write race**: `DiscountRedemption.seq` under `@@unique([codeId, userId, seq])` means two concurrent claims by one shopper both compute `seq 0` and the database refuses the second. An abandoned checkout gives the use back — `releaseOrder` releases the code beside the points reversal — by **deleting** the redemption row rather than writing a reversal, deliberately unlike the append-only loyalty ledger, since nothing financial happened and deleting frees the shopper's `seq` slot. One additive migration (two tables, one enum); no column added to `Order`. Merged via PR #152, merge `5b57ac1`; `gates` and `deploy-staging` both green. **Every one of build-notes' eight "known-shaky" areas was resolved live, not assumed**: both concurrency guarantees — the global usage-cap race and the per-customer race — actually ran under `Promise.all` against real staging Postgres (previously "reasoned, not observed"); `NULL`-decrement/increment semantics for an unlimited code confirmed directly; the stacking arithmetic's exact `200 + 300 = 500` composition proven on a real order, not just in isolated unit tests; the checkout form, refusal rendering and the full `/staff/discounts` surface all rendered and driven live for the first time via `npm run preview` and real sessions, not merely type-checked; and **the admin cross-vendor replay proven in both directions** — closing, for discount codes, the exact gap **#141** recorded for P5a's loyalty config (a platform-ADMIN account transcends vendor membership entirely, so no dedicated SriMart-admin account was even needed — see the comment left on #141). **One real defect found and fixed at Validate, not deferred**: the confirmation email and order-detail pages hardcoded the combined-discount line as "Loyalty points" unchanged from P5a, so a code-only discount rendered as if the shopper's loyalty balance had been spent; arithmetic was always correct, only the label was wrong — relabelled to "Discount" in both places, re-verified live post-fix (`fix(p5b)` `9f9886a`). Deferred with issues filed: category/product-scoped codes (**#146**), auto-applied promotions (**#147**), multiple codes per order (**#148**), first-order-only eligibility (**#149**), naming the applied code on order pages and in the email (**#150**, paired with #138 — same components, one slice not two passes), and code-use reversal on refund (**#151**, paired with #137 — both unreachable today for the identical structural reason, both waiting on ADR-005's refund path). **P5 does NOT close on this merge**: **#143** (P5a live but dark) must be resolved first — loyalty and discounts are both in production but currently unreachable by any customer, and closing the phase before then would misstate what a shopper can actually do. | Second and final P5 slice; phase awaits #143, not a promotion |
| 2026-08-12 | **P5b promoted to production** (PR #155): `staging → main`, merge `1b66bdf`; `gates` green on the promotion (48s). **Unlike P5a's promotion, this one carried a genuinely pending migration** — P5b was validated entirely against staging (`.env`/`.dev.vars`/`secrets/staging.vars` agreed, verified at Orient), so `deploy-production`'s log shows `Applying migration `20260811175844_p5b_discount_codes`` rather than "no pending migrations," and that log line is what was checked, not just the merge or the green tick. Purely additive (`DiscountCode`, `DiscountRedemption`, one enum; no column on `Order`), so no backfill window. `/api/health` confirmed `commit: 1b66bdf`, `db.ok: true` post-deploy. Production had zero `DiscountCode` rows on merge — addressed by the re-seed below, not by this PR. | Promotion of #145; unblocks #143's resolution, which needed the `DiscountCode` table to exist first |
| 2026-08-12 | **P5 closed — #143 resolved.** `prisma/seed.ts` upserts `WELCOME10` in the same function that sets `loyaltyEnabled`, so the re-seed that fixes #143 could not run before P5b's migration landed — it would have committed Aheed's config and tier rows, then thrown `P2021` on the missing `DiscountCode` table. With P5b promoted, `npm run db:seed` ran against production with `SEED_AHEED_HOST` set and `SEED_SRIMART_HOST` **deliberately omitted**, so `main()` skipped SriMart's entire block — confirmed after the run: SriMart's `VendorConfig.loyaltyEnabled` still `false`. Aheed received the seed's placeholder rates as-is (1pt/£1, 1p/pt, 100pt minimum, 30-day tier window, 12-month expiry), Silver (£50 spend, 1.25×) and Gold (£100 spend, 1.5×), and `WELCOME10` (10% off £15+, unlimited total uses, 1 per customer) — an owner decision, not a default carried through mechanically. All three read back live from production via a throwaway, uncommitted verification script; product images were independently confirmed reachable at the production CDN with the correct placeholder content, ruling out the S3-vars-pointing-at-staging trap the runbook called out in advance. Catalogue, inventory, orders and the loyalty ledger are untouched — `seedCatalogue` skips any category already present per vendor, and the seed writes no order/ledger rows. **Two real defects found during the run, both filed rather than fixed in place** (`secrets/production.vars` is gitignored): **#154** (`CDN_BASE_URL` held staging's host; fixed by the owner before this run) and **#156** (the `RESEND_API_KEY` line's malformed `=` spacing broke plain-bash `source`, which also briefly printed the key's plaintext value to a terminal — worth rotating out of caution). **P5 (#88) closes here**: both loyalty and discounts are now live *and reachable* by a customer, which is what P5a's and P5b's own promotion rows explicitly withheld. | Closes P5; both halves shipped and dark since 2026-08-11, now on and reachable |
