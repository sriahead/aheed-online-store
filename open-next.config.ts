import { defineCloudflareConfig } from "@opennextjs/cloudflare";

// Minimal walking-skeleton config. Add caching (KV/R2 incremental cache),
// queues, etc. later — behind ports, per specs/architecture.md.
export default defineCloudflareConfig({});
